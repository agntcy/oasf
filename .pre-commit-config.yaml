repos:
  - repo: local
    hooks:
      - id: fmt
        name: fmt
        description: Run task fmt to format proto, schema, and Elixir files.
        entry: task fmt
        language: system
        pass_filenames: false
